# WinPeripheralEmu
A Windows mouse and keyboard emulated as a bluetooth peripheral for use across devices. The small app window allows for edge selection to transfer to second device. The software allows for return of mouse cursor (and the keyboard).
